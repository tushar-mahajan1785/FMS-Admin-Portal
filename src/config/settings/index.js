const Settings = {
  drawerWidth: 280,
  appBarPosition: "fixed",

  // New sidebar settings
  sidebar: {
    defaultOpen: true,       // show sidebar by default on desktop
    allowDesktopToggle: true // allow toggle open/close on desktop
  },
  logo: {
    path: "/fms_logo.jpg",  // local/public path
    alt: "",
    height: 40,                // customize size
  }
};

export default Settings;
